Maximum Number of 1s in a Row.
=================
int maxOne(int mat[R][C], int m, int n)
{
    
    int count; int maxM =  INT_MIN;
    for(int i = 0; i < R; i++)
    {
        count = 0;
        for(int j = 0; j < C; j++)
        {   
            if(mat[i][j] == 1) count++;
        }   
        maxM = max(maxM, count); }    
    return maxM; }

=================
